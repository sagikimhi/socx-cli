---
regression:
  path: "@path @format {this.paths.APP_ROOT_DIR}/assets/rgr"
  test_cls: "socx.regression.test:Test"
  max_runs_in_parallel: 10

  run:
    input:
      filename: failed_tests_history.log
      directory: "@path @format {this.regression.path}/inputs"
    output:
      directory: "@path workrun/regression"

  runtime:
    path: "@path @format {this.regression.path}/nightly"
    logs:
      directory: logs
    files:
      - filename: socRgrDB.pyclass
        name: soc_rgr_db_pickled
